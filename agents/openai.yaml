interface:
  display_name: "Start Work"
  short_description: "Coordinate roster-routed dev agents"
  default_prompt: "Use $start-work to initialize or reuse a project-level Manager, Developer, and Reviewer team with roster-based handoffs, run ledgers, direct thread messages when available, and callback/manual relay fallback."
